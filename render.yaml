services:
  - type: web
    name: quiz-contest-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    # IMPORTANT: No publish directory for Node.js apps
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
